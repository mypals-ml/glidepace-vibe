# Task 123 Plan

## Implementation
- Add a local search query to the `Group by Fields` dialog.
- Filter available project fields by field name or data type.
- Keep selected fields visible and unchanged while filtering the available list.
- Show distinct empty states for projects with no fields and searches with no matches.

## Verification
- Run lint, type-check, build, and the test suite.
