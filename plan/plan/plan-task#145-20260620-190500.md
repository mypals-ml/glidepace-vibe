# Implementation Plan: Task #145 — Refactor large TimelineChart file

## Findings

- Largest file: `TimelineChart.tsx` (~1066 lines)
- Splittable without behavior change: constants, zoom gestures, group/task rows, context menu, group helpers

## Extraction plan

1. `timelineChartConstants.ts` — layout constants and viewport helper
2. `timelineChartUtils.ts` — `getGroupWeightedProgress`, `computeGroupRowSpans`, `getTimelineTaskBarLayout`
3. `useTimelineChartZoomGestures.ts` — wheel/pinch zoom and scroll re-anchor
4. `TimelineChartContextMenu.tsx` — gantt context menu overlay
5. `TimelineGroupRow.tsx` — group card row rendering
6. `TimelineTaskRow.tsx` — task bar row rendering

## Verification

- `npm test -- src/components/Dashboard/Views/TimelineChart.test.tsx`
- `npm run lint`
- `npm run type-check`
- `npm run build`