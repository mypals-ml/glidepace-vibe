# Walkthrough: Task #130

Created By: Codex

## Brief Execution Walkthrough

Fixed estimate-save optimistic updates so Gantt task bars refresh immediately after task detail Estimate changes; verified with focused tests, lint, type-check, build, and local mock browser measurement.

## Task

- Task: #130 Bug: The Gantt char task bar won't change after the modification of the Estimate value in the Task Details view
- Board state claimed: Todo -> In progress

## Implementation Details

- Added `applyOptimisticTaskDateUpdate` in `src/hooks/dashboardTasks/taskFieldHelpers.ts`.
- Replaced the inline optimistic date/estimate update block in `src/hooks/dashboardTasks/useTaskMutations.ts` with the helper.
- Estimate and estimate-unit changes now:
  - Recalculate the local `targetDate` immediately from the effective start date, estimate, and unit.
  - Clear stale `tempTargetDate` values so Gantt layout no longer prefers an old dependency-derived target date.
  - Preserve `tempStartDate` when the start date was not explicitly changed.
  - Set `localUpdateTimestamp` for estimate and unit edits so short-lived reconciliation cannot overwrite the local optimistic result.
- Updated `docs/FEATURES.md` to document that Estimate edits immediately update Gantt task bars from optimistic local state.

## Verification

- Ran focused unit tests:
  - `npm test -- --run src/hooks/dashboardTasks/taskFieldHelpers.test.ts`
- Ran required project checks:
  - `npm run lint`
  - `npm run type-check`
  - `npm run build`
- Browser verification:
  - Started `npm run dev:test -- --host 127.0.0.1`.
  - Opened the local mock app, selected the mock `Demo: Bug Tracker` project, opened the Gantt/task detail view, and edited a task Estimate.
  - Measured the first Gantt task bar before and after the edit. Width changed from about `122px` to about `245px` without a project refresh, confirming the Gantt bar responded to the saved estimate change.
