# Walkthrough: Task #174 (refinement)

**Created By:** Grok

## Brief execution walkthrough

Start link handle stays hidden until link drag; no hover visual when idle. Tests/lint/build passed.

## Refinement request (last comment)

When hovering the start link point on a gantt task bar, if not currently establishing a link, do not change its state or visual effect.

## Changes made

### `TimelineTaskRow.tsx`
- Added `isEstablishingLink` (`Boolean(linkDragSourceTaskId)`)
- Start link handle now stays `opacity-0 pointer-events-none` unless a link drag is in progress
- Drop-target and hover-target visuals only appear while establishing a link
- Removed `group-hover:opacity-100` and `hover:scale-125` from the idle start connector
- End link handle unchanged (still reveals on row hover for drag-to-link)

### `TimelineChart.test.tsx`
- Updated pointer-events regression for idle start handle
- Added test: start handle hidden on hover when no link drag
- Updated grab/crosshair test to assert during active link drag

## Verification

```bash
npm test -- --run src/components/Dashboard/Views/TimelineChart.test.tsx
npm run lint
npm run type-check
npm run build
```

- Commit: `27d19b1`
- Pushed to `origin/develop`
- Board status: In review