# Walkthrough: Task #174

**Created By:** Grok

## Brief execution walkthrough

Start link handle uses grab cursor like task bar; tests/lint/build passed.

## Details

- Changed the left/start gantt link connector from `cursor-crosshair` to `cursor-grab` to match the task bar body.
- End connector keeps `cursor-crosshair` for drag-to-link.
- Updated regression tests. Commit `e5c6d76` (shared with #173).