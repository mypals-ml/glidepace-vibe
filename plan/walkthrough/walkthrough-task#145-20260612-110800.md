# Walkthrough — Task #145: Refactor one big source code files if any

Run: 2026-06-12 11:08 (Claude Automation)

## Brief execution walkthrough

Split TaskSidebar.tsx (1940 -> 1214 lines) into tree/dnd/row modules + unit tests. Verified: node assertions, lint, type-check.

## What was implemented

Task #145 asks to find one source file that is too big, split it with a test plan, verify, and finish. A previous iteration split `src/i18n.ts` into `src/locales/` (commit 289afa4). This iteration targets the largest remaining file.

### File selection

Line counts of the largest source files before the change:

- `src/components/Dashboard/TaskSidebar.tsx` — 1940 lines (selected)
- `src/hooks/useDashboardTasks.ts` — 1540 lines
- `src/components/Dashboard/TaskDetailsPanel.tsx` — 1229 lines
- `src/lib/githubMock.ts` — 1088 lines

`TaskSidebar.tsx` was selected because it is the largest and has a clean seam: pure tree-layout helpers and three self-contained row subcomponents that the main component only consumes.

### Split

All moved code is verbatim (no behavior changes), only module boundaries and imports changed:

1. **`src/components/Dashboard/taskSidebarTree.ts` (new, 101 lines)** — pure, unit-testable tree layout logic:
   - Constants: `TREE_NODE_BASE_X`, `TREE_DEPTH_STEP`, `TREE_CONTENT_GAP`, `TREE_ELBOW_HEIGHT`, `TREE_ELBOW_RADIUS`, `TREE_ROW_HEIGHT`, `TREE_ROW_PADDING_LEFT`
   - Types: `TreeRowMeta`, `TreeGuideSegment`, `DashboardTreeRow`
   - Functions: `getDashboardItemTreeDepth`, `getTreeNodeX`, `getTreeDragHandleX`, `getTreeRowDividerLeft`, `buildDashboardTreeRows`

2. **`src/components/Dashboard/taskSidebarDnd.ts` (new, 35 lines)** — dnd-kit sensor customizations:
   - `TaskMouseSensor`, `TaskTouchSensor`, `blurActiveDragHandle` (plus internal `eventTargetElement`)

3. **`src/components/Dashboard/TaskSidebarRows.tsx` (new, 617 lines)** — presentational row components:
   - `TreeTitleCell` (internal), `TaskGroupRow`, `SortableTaskRow`
   - `ContextMenuTarget` type (consumed by the main component's context-menu state)
   - Assignee chip class constants (only used by `SortableTaskRow`)

4. **`src/components/Dashboard/TaskSidebar.tsx` (1940 -> 1214 lines)** — now only the main `TaskSidebar` component, its context menu, group editor, field-group dialog, and DnD orchestration; imports the three new modules. Unused imports (`useSortable`, `CSS`, `AssigneePicker`, `StatusPicker`, status colors, tree colors, `memo`, event types, etc.) were removed.

5. **`src/components/Dashboard/taskSidebarTree.test.ts` (new, 150 lines)** — Vitest unit tests for the extracted pure logic: depth resolution (group depth, explicit task depth, groupPath fallback), node/divider/drag-handle geometry incl. clamping, and `buildDashboardTreeRows` guide-segment construction (rows with children, middle vs. last sibling rails, nested subtrees, flat lists, empty input).

### Test plan and verification

Test plan (made before splitting): keep all moved code verbatim; confirm via identifier-usage audit that the main component body references none of the moved internals except through the new exports; preserve the module-scope dnd-kit mocks in the existing `TaskSidebar.test.tsx` (they apply across the import graph, so extracted modules stay mocked); then run type-check, lint, unit assertions on the extracted logic, and the build.

Executed verification:

- `npm run type-check` (`tsc -b`) — passed, no errors.
- `npm run lint` (`eslint .`) — passed, no warnings/errors.
- Unit assertions: `taskSidebarTree.ts` compiled with `tsc` to CommonJS and exercised with a standalone node assertion script mirroring `taskSidebarTree.test.ts` (depth helpers, geometry helpers, and 5 `buildDashboardTreeRows` scenarios) — all assertions passed.
- `npx vitest run` and `npm run build` could not be executed in the sandbox: node_modules was installed on macOS and the Linux sandbox lacks `@rollup/rollup-linux-arm64-gnu`; installing it was blocked by the network proxy (403, npm registry unreachable). The TypeScript half of the build (`tsc -b`) passed. The new `taskSidebarTree.test.ts` will run with the project's normal `npm run test` on the host.

### Notes

- No user-facing feature changes, so `docs/FEATURES.md` was not modified.
- `git pull`/`git push` to github.com fail in the sandbox (HTTP 403 from proxy); the commit is local on `develop` awaiting push.
