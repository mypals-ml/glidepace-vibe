# Task #144 Walkthrough

## Brief execution walkthrough

Kept the Gantt dependency break-link hover button inside the visible chart viewport by passing scroll viewport data into dependency-line rendering and clamping the button position; verified with focused tests, type-check, and production build.

## Task

- GitHub issue: #144
- Title: refine: unlink button on the link line in the gantt chart view
- Requirement: when hovering a Gantt dependency link line, keep the unlink button visible inside the viewport.

## Implementation

- Extended the Gantt chart viewport state to track `scrollLeft`, `scrollTop`, `clientWidth`, and `clientHeight`.
- Added a shared `getViewportInfo` helper in `GanttChart.tsx` and used it anywhere viewport state is refreshed, including normal scroll, initial centering, zoom re-anchoring, and pinch/wheel zoom.
- Passed `viewportInfo` into `DependencyLines`.
- Added viewport-aware break-link button placement in `DependencyLines.tsx`.
  - The dependency path geometry is unchanged.
  - The hover `foreignObject` center is clamped horizontally and vertically inside the visible chart viewport.
  - The existing 24px break-link button size is reused for clamp margins.
- Added tests in `DependencyLines.test.tsx` for left/top and right-edge viewport clamping.
- Updated `docs/FEATURES.md` to document the viewport-clamped break-link hover button.

## Verification

- `PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm test -- DependencyLines.test.tsx --run`
  - Passed: 1 file, 8 tests.
- `PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run type-check`
  - Passed.
- `PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run build`
  - Passed.
  - Vite reported the existing large chunk warning.
- `PATH="/opt/homebrew/opt/node@22/bin:$PATH" npm run lint`
  - Did not complete: ESLint hung for several minutes with no diagnostic output and was stopped.
- Targeted ESLint on changed source/test files was also attempted and hung without diagnostic output, then was stopped.

## Notes

- Existing unrelated working-tree changes were detected in `src/components/Dashboard/Views/GanttChart.tsx` and `src/components/Dashboard/Views/GanttChart.test.tsx` for group-card title layout. They were not included in this task commit.
