# Walkthrough: Task #170

**Created By:** Grok

## Brief execution walkthrough

Gantt drag row lift, hidden link handles, crosshair cursors, removed mobile Move menu; tests/lint/build passed.

## Task

- GitHub issue: https://github.com/mypals-ml/glidepace-vibe/issues/170
- Refine gantt vertical drag visuals and link-handle behavior.

## Changes made

### `src/components/Dashboard/Views/TimelineChart.tsx`

- Applied sidebar-style lifted row styling (`z-50 shadow-lg ring-1 ring-primary/20 bg-white`) while a task bar is moving vertically.
- Hid start/end link connector nodes whenever that task bar drag is active.
- Changed the end connector cursor from grab to crosshair (start connector already used crosshair).
- Removed the mobile gantt context-menu **Move task** action and the unused `mobileMoveTaskId` touch-drag flow.

### `src/components/Dashboard/Views/TimelineChart.test.tsx`

- Updated vertical-drag test to assert lifted row classes.
- Replaced mobile move-menu test with a regression that Move is absent.
- Added tests for hidden link handles during drag and crosshair cursors on connectors.

### `docs/FEATURES.md`

- Documented gantt drag visuals, hidden link handles, crosshair cursors, and mobile move-menu removal.

## Verification

```bash
npm test -- --run src/components/Dashboard/Views/TimelineChart.test.tsx  # 18 passed
npm run lint                                                          # passed
npm run type-check                                                    # passed
npm run build                                                         # passed
```

- Commit: `b23060f`
- Pushed to `origin/develop`
- Board status: In progress → In review

## Summary

Task #170 completed. Gantt vertical drags now mirror task-list movement styling, link handles stay out of the way during drags, connector hover uses a crosshair cursor, and mobile gantt no longer offers Move from the context menu. No blockers.