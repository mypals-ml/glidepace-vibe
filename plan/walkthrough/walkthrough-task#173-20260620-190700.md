# Walkthrough: Task #173

**Created By:** Grok

## Brief execution walkthrough

Gantt drag uses primary fill, highlights drop row; tests/lint/build passed.

## Details

- Added `getGanttDropRowIndex()` to compute destination row during vertical drags.
- Moving row fill: `bg-primary/[0.04]` with lifted ring/shadow.
- Destination row: `bg-primary/[0.06] ring-2 ring-inset ring-primary/30` on task and group rows.
- Commit `e5c6d76` (shared with #174).