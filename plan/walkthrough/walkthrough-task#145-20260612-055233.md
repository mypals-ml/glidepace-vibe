# Walkthrough — Task #145: Refactor one big source code files if any

## Brief execution walkthrough

Claude Automation committed the changes 2026-06-12 05:52 UTC | Split 1188-line src/i18n.ts into per-locale modules src/locales/{en,ja,zh-CN,zh-TW}.ts; resources verified deep-equal, i18next smoke test, type-check+lint pass.

## Task

- Board task: #145 "Refactor one big source code files if any" (group: GeneralOptimize)
- Description loop: find a source file that is too big per best practice, make a test plan, split it, run the tests, then finish.

## File selection

Largest project source files (excluding node_modules/build output):

| File | Lines |
|---|---|
| src/components/Dashboard/TaskSidebar.tsx | 1940 |
| src/hooks/useDashboardTasks.ts | 1536 |
| src/components/Dashboard/TaskDetailsPanel.tsx | 1229 |
| src/i18n.ts | 1188 |

`src/i18n.ts` was chosen: it inlined all four locale resource blocks (en, ja, zh-CN, zh-TW) in one file — the file itself even carried a comment noting the resources should be modularized. Splitting it is a purely mechanical, low-risk refactor that directly serves the RULES.md "Separation of Concerns" rule, whereas splitting the large stateful React components (TaskSidebar/TaskDetailsPanel) carries much higher regression risk for an automated run and is better done with full vitest available (see Blockers).

## Test plan (made before splitting)

1. Byte-level verification that each extracted locale block is identical to the corresponding block in the original file (no accidental edits).
2. Deep-equality check that the reassembled `resources` object equals the original inline `resources` object from git HEAD.
3. Functional smoke test with the real `i18next` package: init with the new modular resources, assert `t()` lookups in all four locales, including interpolation (`app.syncedMinutesAgo` with count) and language switching.
4. `npm run type-check` (tsc -b) — must pass.
5. `npm run lint` (eslint) — must pass.
6. `npm run test` (vitest) and `npm run build` — attempted; see Blockers.

## What was implemented

- Created `src/locales/en.ts` (310 lines), `src/locales/ja.ts` (289), `src/locales/zh-CN.ts` (289), `src/locales/zh-TW.ts` (289). Each exports its locale object (`{ translation: { ... } }`) as default, content extracted verbatim (dedented 2 spaces) from the original inline blocks.
- Rewrote `src/i18n.ts` down to 27 lines: imports the four locale modules, assembles `resources`, and keeps the exact same `i18next.init` options (`lng: "en"`, `fallbackLng: "en"`, `escapeValue: false`).
- No public API change: `src/i18n.ts` still default-exports the initialized `i18n` instance; the two importers (`src/App.tsx` side-effect import, `src/lib/githubTaskMapper.ts`) are untouched.

## How it was verified

1. Python script compared every extracted inner line against `git show HEAD:src/i18n.ts` — all four blocks byte-identical ("OK inner content identical").
2. Node script converted the new modules and the old inline `resources` to CJS and ran `assert.deepStrictEqual(newResources, oldResources)` — passed.
3. i18next functional smoke test passed for all four locales:
   - en: `app.name` → "Glidelines", `app.syncedMinutesAgo` with count 5 → "Synced 5 minutes ago"
   - ja: `app.syncNow` → "たった今同期…" variant ("今すぐ同期")
   - zh-CN: `about.title` non-empty
   - zh-TW: `help.closeWindow` → "關閉視窗"
4. `npm run type-check` — passed (exit 0, no output).
5. `npm run lint` — passed (exit 0, no output).
6. Key-parity audit: ja/zh-CN/zh-TW each have 259 keys, en has 280; the 21 missing keys are pre-existing (content is byte-identical to the old file) and fall back to en at runtime. Not introduced or changed by this refactor.

## Commit

- `289afa4` refactor: split locale resources out of src/i18n.ts into src/locales/ (#145) — amended to include this walkthrough file.

## Blockers / notes

- `git pull` / `git push` and `npm install` are blocked in the automation sandbox (HTTP 403 from proxy). The branch was already up to date with origin/develop; the commit is local and needs a manual `git push origin develop`.
- `npm run test` (vitest) and `npm run build` (vite) could not run in the sandbox: node_modules was installed on macOS (darwin-arm64) and the Linux sandbox lacks the platform-native rollup/esbuild binaries; installing them is blocked by the same network policy. The change is covered by the deep-equal + i18next smoke verification above; running `npm run verify && npm run test && npm run build` on the host machine before pushing is recommended.
