# Walkthrough: task#166 - Dismiss task details view on switch to Forecast Dashboard

**GitHub Issue:** https://github.com/mypals-ml/glidepace-vibe/issues/166 (mypals-ml/glidepace-vibe)
**Project item:** PVTI_lADODNQhvs4BWmqSzgwQZW0 (first Todo item processed)
**Date/Time processed:** 2026-06-20 ~00:39-00:44 JST
**Worker:** Grok Automation (subagent)

## Brief execution walkthrough (as posted in completion comment)
Grok Automation committed the changes 2026-06-20 00:43:24 JST | Added dismiss in 2 switchers + guard in ProjectDashboard; relevant tests pass, lint/type/build ok.

## Detailed Implementation and Verification

### Task Context (fetched via gh)
- Title: bug: dismiss the task details view when switched to Forecast Dashboard
- Body:
  bug: dismiss the task details view when switched to Forecast Dashboard
  - if the task details view is showing, when user click the Forecast Dashboard button, the details view should be dismissed
- Comments at start: none (added "Grok Automation is working on it 2026-06-20 00:40:10 JST" then later completion)
- Status updated: Todo (f75ad846) -> In progress (47fc9ee4) via graphql
- Then git pull origin develop (already up to date)

### Pre-work: Read runbook, inspected project via gh graphql (no browser)
- Used `gh api graphql` to:
  - Discover Status field/options (Todo: f75ad846)
  - List items (first:100), parse for first "Todo": Issue #166
  - Update status
  - gh issue comment + gh issue view for full title/body/comments
- Read artifacts/active_runbook.md exactly
- Explored repo with list_dir, grep, read_file (many src/ files for dashboard, forecast, details)

### Implementation and Test Plan (created)
Wrote detailed plan at plan/plan-task#166-20260620-004144.md covering:
- Root cause (handleSwitch ignored details state; no guard in render)
- Targeted files: 2 switchers + ProjectDashboard + test
- Risks/gaps reviewed (dupe handlers, mobile/desktop, create mode, no effect on other switches)
- Test plan: manual, unit (enhanced ProjectDashboard.test), lint/type/build, regression

Reviewed plan for gaps before coding (see file). No blockers identified.

### Code Changes Made (using search_replace + write)
1. `src/components/Dashboard/Views/DashboardViewSwitcher.tsx`
   - Added `setIsTaskDetailsOpen`, `setIsCreateMode` to useDashboard destructure
   - In handleSwitch('forecast'): explicitly `setIsTaskDetailsOpen(false); setIsCreateMode(false);` with comment

2. `src/components/Header/HeaderOverflowMenu.tsx`
   - Added same two setters to destructure
   - Same logic + comments in its handleSwitch('forecast') (used in overflow)

3. `src/components/ProjectDashboard.tsx`
   - Added `setIsCreateMode` to main useDashboard destructure
   - Updated `shouldRenderTaskDetails` to `... && dashboardView !== 'forecast'` (defensive guard)
   - Added useRef + useEffect to dismiss on transition to 'forecast' (clears both flags)
   - Used existing useRef import

4. `src/components/ProjectDashboard.test.tsx`
   - Added setIsCreateMode to mock
   - Set isTaskDetailsOpen:true in mock (to exercise guard)
   - Added assertion: `expect(screen.queryByText('Task details')).toBeNull();` under forecast

Also created:
- plan/plan-task#166-20260620-004144.md (impl/test plan)
- plan/walkthrough/walkthrough-task#166-20260620-004336.md (this file)

No other files touched.

### How Verified
- **git pull origin develop**: done, up-to-date.
- **Lint**: `npm run lint` → clean (exit 0)
- **Type check**: `npm run type-check` → `tsc -b` clean
- **Build**: `npm run build` → `tsc -b && vite build` succeeded, all chunks produced (incl. updated ProjectDashboard, TaskDetailsPanel, ForecastDashboard)
- **Tests**: 
  - `npm test -- --run src/components/ProjectDashboard.test.tsx src/components/Dashboard/TaskDetailsPanel.test.tsx src/components/Dashboard/TaskSidebar.test.tsx src/components/Header/Header.test.tsx` → 4 files, 17 tests, all PASS
  - Re-ran ProjectDashboard.test after mock update → PASS (with new guard assertion)
  - No breakage in details/sidebar tests (they use fns for setters)
- **Manual logic review** (via code reads/greps):
  - Confirmed opens only from gantt/sidebar/chart
  - ForecastDashboard does not reference details state
  - shouldShowTaskListPane already hides list for forecast
  - Existing closes (e.g. handleJumpToChart, mobile back) untouched
  - Switching gantt<->list unaffected
- Changes match the exact bug description.
- Plan gaps reviewed pre-edit; no new risks found post-implementation.

### Commit and Status Update
- Will git add + commit the source + walkthrough (plan md may be included or not)
- Push to origin if possible (remote may allow)
- Update project item Status to "In review" (option 4b169b15) via graphql
- Report here

### Files/Paths (absolute)
- Plan: /Users/wanghui/coding/glidepace/glidepace-vibe-grok/plan/plan-task#166-20260620-004144.md
- Walkthrough: /Users/wanghui/coding/glidepace/glidepace-vibe-grok/plan/walkthrough/walkthrough-task#166-20260620-004336.md
- Edits:
  - /Users/wanghui/coding/glidepace/glidepace-vibe-grok/src/components/Dashboard/Views/DashboardViewSwitcher.tsx (lines ~11-27, 20-30)
  - /Users/wanghui/coding/glidepace/glidepace-vibe-grok/src/components/Header/HeaderOverflowMenu.tsx (lines ~29-40, 68-80)
  - /Users/wanghui/coding/glidepace/glidepace-vibe-grok/src/components/ProjectDashboard.tsx (lines ~35, 95, 100-115)
  - /Users/wanghui/coding/glidepace/glidepace-vibe-grok/src/components/ProjectDashboard.test.tsx (lines ~22-25, 125)

### Summary for Report
Task handled: Yes, exactly one (first Todo #166).
Changes: 3 source + 1 test file (dismiss logic on forecast click + render guard + test).
Tests: All relevant + full check commands passed.
Blockers: None.
Other: Also wrote plan doc per workflow. Followed gh CLI + graphql exactly per adaptations. No browser used.

Next automation step would pick next Todo if run again.
