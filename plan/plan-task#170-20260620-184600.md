# Implementation Plan: Task #170 — Gantt drag visual refinements

## Requirements

1. Match task-list row lift styling when dragging a gantt bar vertically.
2. Remove mobile gantt context menu "Move task".
3. Hide link connector nodes while a task bar drag is active.
4. Use crosshair cursor on gantt link connector hover.

## Implementation

- `TimelineChart.tsx`: apply `z-50 shadow-lg ring-1 ring-primary/20 bg-white` to the row when `taskDrag.hasMoved`; remove `mobileMoveTaskId` flow and mobile move menu item; hide link handles when `taskBarDragState` targets the row; set end connector cursor to `cursor-crosshair`.
- `TimelineChart.test.tsx`: update/add regression tests.
- `docs/FEATURES.md`: document behavior changes.

## Verification

- `npm test -- src/components/Dashboard/Views/TimelineChart.test.tsx`
- `npm run lint`
- `npm run type-check`
- `npm run build`