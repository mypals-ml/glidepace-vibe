# Living Task List

## Current Goal
- [x] Initialize the foundational documentation (`TODO.md`, `RULES.md`, `ARCHITECTURE.md`)
- [x] Select core technology stack (Vite + React + Tailwind + Capacitor)
- [x] Scaffold the base web application structure

## Future Features
- [ ] Build "Start Page" for GitHub OAuth browser authentication
- [ ] Implement Project ID input flow (with permanent `localStorage` caching)
- [ ] Build MS Project-style split dashboard (Left: List, Right: Gantt)
- [ ] Implement Light/Dark theme toggle (Default: Bright Mode)
- [ ] Detect "Draft Issues" and provide a "Convert to Issue" UX flow, warning to enable Repository Issues in Settings, and prompting to set the Default Repository (fallback to Read-Only mode)
- [ ] Create `cascade.test.ts` and write pure functions for date math
- [ ] Write GraphQL mutations to sync updated dates back to GitHub
